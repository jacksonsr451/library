import { Request, Response } from "express"
import BookService from "../../../application/usecases/BookService"

const remove = (service: BookService) => async (request: Request, response: Response) => {

}

export default remove
