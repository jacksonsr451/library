import { Request, Response } from "express"
import BookService from "../../../application/usecases/BookService"

const show = (service: BookService) => async (request: Request, response: Response) => {

}

export default show
