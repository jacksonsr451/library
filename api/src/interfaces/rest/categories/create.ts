import { Request, Response } from "express"
import CategoryService from "../../../application/usecases/CategoryServices"

const create = (service: CategoryService) => async (request: Request, response: Response) => {

}

export default create
