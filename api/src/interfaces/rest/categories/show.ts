import { Request, Response } from "express"
import CategoryService from "../../../application/usecases/CategoryServices"

const show = (service: CategoryService) => async (request: Request, response: Response) => {

}

export default show
