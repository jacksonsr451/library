import { Request, Response } from "express"
import CategoryService from "../../../application/usecases/CategoryServices"

const view = (service: CategoryService) => async (request: Request, response: Response) => {

}

export default view
