import { Request, Response } from "express"
import LibraryService from "../../../application/usecases/LibraryService"

const remove = (service: LibraryService) => async (request: Request, response: Response) => {

}

export default remove
