import { Request, Response } from "express"
import LibraryService from "../../../application/usecases/LibraryService"

const view = (service: LibraryService) => async (request: Request, response: Response) => {

}

export default view
