import { Request, Response } from "express"
import RentBookService from "../../../application/usecases/RentBookService"

const create = (service: RentBookService) => async (request: Request, response: Response) => {

}

export default create
