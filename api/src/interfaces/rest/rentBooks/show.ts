import { Request, Response } from "express"
import RentBookService from "../../../application/usecases/RentBookService"

const show = (service: RentBookService) => async (request: Request, response: Response) => {
    
}

export default show
