import { Request, Response } from "express"
import RentBookService from "../../../application/usecases/RentBookService"

const view = (service: RentBookService) => async (request: Request, response: Response) => {

}

export default view
